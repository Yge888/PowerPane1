# 常用命令参考

## 电源管理

| 操作 | 命令 |
|------|------|
| 立即关机 | `shutdown /s /t 0` |
| 定时关机（N秒后） | `shutdown /s /t N` |
| 立即重启 | `shutdown /r /t 0` |
| 定时重启（N秒后） | `shutdown /r /t N` |
| 取消已计划的关机/重启 | `shutdown /a` |
| 休眠 | `rundll32.exe powrprof.dll,SetSuspendState 1,0,0` |
| 睡眠 | `rundll32.exe powrprof.dll,SetSuspendState 0,1,0` |

## 锁屏 & 用户

| 操作 | 命令 |
|------|------|
| 锁屏 | `rundll32.exe user32.dll,LockWorkStation` |
| 切换用户 | `tsdiscon` |

## 网络

| 操作 | 命令 |
|------|------|
| 查看IP配置 | `ipconfig` |
| Ping测试 | `ping 地址` |
| 查看路由表 | `route print` |
| DNS缓存查看 | `ipconfig /displaydns` |
| DNS缓存清除 | `ipconfig /flushdns` |

## 进程 & 系统

| 操作 | 命令 |
|------|------|
| 任务管理器 | `taskmgr` |
| 查看进程列表 | `tasklist` |
| 结束进程 | `taskkill /im 进程名.exe /f` |
| 系统信息 | `msinfo32` |
| 注册表编辑器 | `regedit` |
| 服务管理 | `services.msc` |

## 磁盘 & 文件

| 操作 | 命令 |
|------|------|
| 查看磁盘使用 | `wmic logicaldisk get size,freespace,caption` |
| 磁盘检查 | `chkdsk C:` |
| 磁盘清理 | `cleanmgr` |
| 查看文件/目录 | `dir` |
| 强制删除目录 | `rmdir /s /q 目录名` |

## PowerShell 增强命令

| 操作 | 命令 |
|------|------|
| 带定时的电源操作 | `.\power-timer.ps1 -Action shutdown -Delay 300` |
| 重启 | `.\power-timer.ps1 -Action restart -Delay 60` |
| 锁屏 | `.\power-timer.ps1 -Action lock` |
| 取消 | `.\power-timer.ps1 -Action cancel` |
